---
title: API Reference
description: API Docs for https://briefkastenhq.com/api
---

import { Root, API, APIInfo, APIExample, Property } from 'fumadocs-ui/components/api'
import { Tabs, Tab } from 'fumadocs-ui/components/tabs'
import { Accordion, Accordions } from 'fumadocs-ui/components/accordion';

<Root>

<API>

<APIInfo method={"GET"} route={"/bookmarks"}>

## Search for bookmarks

### Query Parameters

<Property required={true} deprecated={false} name={"query"} type={"string"}>

Search query string

<span>Example:  `"Tech blogs"`</span>

<span>Format:  `"string"`</span>

</Property>

| Status code | Description |
| ----------- | ----------- |
| `200` | Successful |
| `401` | Unauthorized |
| `403` | Forbidden |
| `429` | Too Many Requests |

</APIInfo>

<APIExample>

```bash title="curl"
curl -X GET "http://briefkastenhq.com/bookmarks"
```

<Tabs items={["200"]}>

<Tab value={"200"}>

List of bookmark results

```json title="Example Response"
[
  {
    "id": "cjld2cjxh0000qzrmn831i7rn",
    "userId": "cjld2cyuq0000t3rmniod1foy",
    "title": "Tech Blog",
    "url": "https://engadget.com",
    "category?": "string",
    "desc?": "string",
    "tags?": [
      {
        "id": "tz4a98xxat96iws9zmbrgj3a",
        "userId": "q6jy42jq92xypbao731oj7x8",
        "name": "string"
      }
    ]
  }
]
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
/**
 * List of bookmark results
 */
export type Response = {
  id?: string;
  /**
   * Linked user id
   */
  userId?: string;
  /**
   * Page or link title
   */
  title: string;
  /**
   * Link to the bookmark
   */
  url: string;
  /**
   * Category grouping for your bookmark
   */
  "category?"?: string;
  /**
   * Description of the bookmark
   */
  "desc?"?: string;
  "tags?"?: {
    id?: string;
    userId?: string;
    name: string;
  }[];
}[];

```

</Accordion>

</Accordions>

</Tab>

</Tabs>

</APIExample>

</API>

<API>

<APIInfo method={"POST"} route={"/bookmarks"}>

## Create bookmarks

### Request Body

The bookmark to create

<Property required={false} deprecated={false} name={"id"} type={"string"}>



<span>Example:  `"cjld2cjxh0000qzrmn831i7rn"`</span>

<span>Format:  `"cuid"`</span>

</Property>

<Property required={false} deprecated={false} name={"userId"} type={"string"}>

Linked user id

<span>Example:  `"cjld2cyuq0000t3rmniod1foy"`</span>

<span>Format:  `"cuid"`</span>

</Property>

<Property required={true} deprecated={false} name={"title"} type={"string"}>

Page or link title

<span>Example:  `"Tech Blog"`</span>

</Property>

<Property required={true} deprecated={false} name={"url"} type={"string"}>

Link to the bookmark

<span>Example:  `"https://engadget.com"`</span>

</Property>

<Property required={false} deprecated={false} name={"category?"} type={"string"}>

Category grouping for your bookmark

</Property>

<Property required={false} deprecated={false} name={"desc?"} type={"string"}>

Description of the bookmark

</Property>

<Property required={false} deprecated={false} name={"tags?"} type={"array of object"}>



<Accordions>

<Accordion title={"Object Type"}>

<Property required={false} deprecated={false} name={"id"} type={"string"}>



<span>Example:  `"tz4a98xxat96iws9zmbrgj3a"`</span>

<span>Format:  `"cuid"`</span>

</Property>

<Property required={false} deprecated={false} name={"userId"} type={"string"}>



<span>Example:  `"q6jy42jq92xypbao731oj7x8"`</span>

<span>Format:  `"cuid"`</span>

</Property>

<Property required={true} deprecated={false} name={"name"} type={"string"}>



</Property>

</Accordion>

</Accordions>

</Property>

| Status code | Description |
| ----------- | ----------- |
| `200` | Successful |
| `401` | Unauthorized |
| `403` | Forbidden |
| `429` | Too Many Requests |

</APIInfo>

<APIExample>

```bash title="curl"
curl -X POST "http://briefkastenhq.com/bookmarks" \
  -d '{
  "id": "cjld2cjxh0000qzrmn831i7rn",
  "userId": "cjld2cyuq0000t3rmniod1foy",
  "title": "Tech Blog",
  "url": "https://engadget.com",
  "category?": "string",
  "desc?": "string",
  "tags?": [
    {
      "id": "tz4a98xxat96iws9zmbrgj3a",
      "userId": "q6jy42jq92xypbao731oj7x8",
      "name": "string"
    }
  ]
}'
```

<Tabs items={["200"]}>

<Tab value={"200"}>



```json title="Example Response"
{
  "id": "cjld2cjxh0000qzrmn831i7rn",
  "userId": "cjld2cyuq0000t3rmniod1foy",
  "title": "Tech Blog",
  "url": "https://engadget.com",
  "category?": "string",
  "desc?": "string",
  "tags?": [
    {
      "id": "tz4a98xxat96iws9zmbrgj3a",
      "userId": "q6jy42jq92xypbao731oj7x8",
      "name": "string"
    }
  ]
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export interface Response {
  id?: string;
  /**
   * Linked user id
   */
  userId?: string;
  /**
   * Page or link title
   */
  title: string;
  /**
   * Link to the bookmark
   */
  url: string;
  /**
   * Category grouping for your bookmark
   */
  "category?"?: string;
  /**
   * Description of the bookmark
   */
  "desc?"?: string;
  "tags?"?: {
    id?: string;
    userId?: string;
    name: string;
  }[];
}

```

</Accordion>

</Accordions>

</Tab>

</Tabs>

</APIExample>

</API>

<API>

<APIInfo method={"DELETE"} route={"/bookmarks"}>

## Delete bookmarks

### Request Body

Bookmark ID to delete

<Property required={false} deprecated={false} name={"id"} type={"string"}>



<span>Example:  `"cjld2cjxh0000qzrmn831i7rn"`</span>

<span>Format:  `"cuid"`</span>

</Property>

| Status code | Description |
| ----------- | ----------- |
| `204` | No Content |
| `401` | Unauthorized |
| `403` | Forbidden |
| `429` | Too Many Requests |

</APIInfo>

<APIExample>

```bash title="curl"
curl -X DELETE "http://briefkastenhq.com/bookmarks" \
  -d '{
  "id": "cjld2cjxh0000qzrmn831i7rn"
}'
```



</APIExample>

</API>

<API>

<APIInfo method={"GET"} route={"/tags"}>

## Search for tags

### Query Parameters

<Property required={false} deprecated={false} name={"query"} type={"string"}>

Query

</Property>

| Status code | Description |
| ----------- | ----------- |
| `200` | Successful |
| `401` | Unauthorized |
| `403` | Forbidden |
| `429` | Too Many Requests |

</APIInfo>

<APIExample>

```bash title="curl"
curl -X GET "http://briefkastenhq.com/tags"
```

<Tabs items={["200"]}>

<Tab value={"200"}>

List of tag results

```json title="Example Response"
[
  {
    "id": "tz4a98xxat96iws9zmbrgj3a",
    "userId": "q6jy42jq92xypbao731oj7x8",
    "name": "string"
  }
]
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
/**
 * List of tag results
 */
export type Response = {
  id?: string;
  userId?: string;
  name: string;
}[];

```

</Accordion>

</Accordions>

</Tab>

</Tabs>

</APIExample>

</API>

<API>

<APIInfo method={"POST"} route={"/tags"}>

## Create tag

### Request Body

The tag to create

<Property required={false} deprecated={false} name={"id"} type={"string"}>



<span>Example:  `"tz4a98xxat96iws9zmbrgj3a"`</span>

<span>Format:  `"cuid"`</span>

</Property>

<Property required={false} deprecated={false} name={"userId"} type={"string"}>



<span>Example:  `"q6jy42jq92xypbao731oj7x8"`</span>

<span>Format:  `"cuid"`</span>

</Property>

<Property required={true} deprecated={false} name={"name"} type={"string"}>



</Property>

### Query Parameters

<Property required={false} deprecated={false} name={"query"} type={"string"}>

Query

</Property>

| Status code | Description |
| ----------- | ----------- |
| `200` | Successful |
| `401` | Unauthorized |
| `403` | Forbidden |
| `429` | Too Many Requests |

</APIInfo>

<APIExample>

```bash title="curl"
curl -X POST "http://briefkastenhq.com/tags" \
  -d '{
  "id": "tz4a98xxat96iws9zmbrgj3a",
  "userId": "q6jy42jq92xypbao731oj7x8",
  "name": "string"
}'
```

<Tabs items={["200"]}>

<Tab value={"200"}>



```json title="Example Response"
{
  "id": "tz4a98xxat96iws9zmbrgj3a",
  "userId": "q6jy42jq92xypbao731oj7x8",
  "name": "string"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export interface Response {
  id?: string;
  userId?: string;
  name: string;
}

```

</Accordion>

</Accordions>

</Tab>

</Tabs>

</APIExample>

</API>

<API>

<APIInfo method={"DELETE"} route={"/tags"}>

## Delete tag

### Request Body

Tag ID to delete

<Property required={false} deprecated={false} name={"id"} type={"string"}>



<span>Example:  `"cjld2cjxh0000qzrmn831i7rn"`</span>

<span>Format:  `"cuid"`</span>

</Property>

| Status code | Description |
| ----------- | ----------- |
| `204` | No Content |
| `401` | Unauthorized |
| `403` | Forbidden |
| `429` | Too Many Requests |

</APIInfo>

<APIExample>

```bash title="curl"
curl -X DELETE "http://briefkastenhq.com/tags" \
  -d '{
  "id": "cjld2cjxh0000qzrmn831i7rn"
}'
```



</APIExample>

</API>

<API>

<APIInfo method={"GET"} route={"/categories"}>

## Search for categories

### Query Parameters

<Property required={false} deprecated={false} name={"query"} type={"string"}>

Query

</Property>

| Status code | Description |
| ----------- | ----------- |
| `200` | Successful |
| `401` | Unauthorized |
| `403` | Forbidden |
| `429` | Too Many Requests |

</APIInfo>

<APIExample>

```bash title="curl"
curl -X GET "http://briefkastenhq.com/categories"
```

<Tabs items={["200"]}>

<Tab value={"200"}>

List of categories results

```json title="Example Response"
[
  {
    "id": "pfh0haxfpzowht3oi213cqos",
    "userId": "nc6bzmkmd014706rfda898to",
    "name": "string",
    "description": "string"
  }
]
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
/**
 * List of categories results
 */
export type Response = {
  id?: string;
  userId?: string;
  /**
   * Name of your category
   */
  name: string;
  /**
   * Description for the category
   */
  description?: string;
}[];

```

</Accordion>

</Accordions>

</Tab>

</Tabs>

</APIExample>

</API>

<API>

<APIInfo method={"POST"} route={"/categories"}>

## Create categories

### Request Body

The category to create

<Property required={false} deprecated={false} name={"id"} type={"string"}>



<span>Example:  `"pfh0haxfpzowht3oi213cqos"`</span>

<span>Format:  `"cuid"`</span>

</Property>

<Property required={false} deprecated={false} name={"userId"} type={"string"}>



<span>Example:  `"nc6bzmkmd014706rfda898to"`</span>

<span>Format:  `"cuid"`</span>

</Property>

<Property required={true} deprecated={false} name={"name"} type={"string"}>

Name of your category

</Property>

<Property required={false} deprecated={false} name={"description"} type={"string"}>

Description for the category

</Property>

### Query Parameters

<Property required={false} deprecated={false} name={"query"} type={"string"}>

Query

</Property>

| Status code | Description |
| ----------- | ----------- |
| `200` | Successful |
| `401` | Unauthorized |
| `403` | Forbidden |
| `429` | Too Many Requests |

</APIInfo>

<APIExample>

```bash title="curl"
curl -X POST "http://briefkastenhq.com/categories" \
  -d '{
  "id": "pfh0haxfpzowht3oi213cqos",
  "userId": "nc6bzmkmd014706rfda898to",
  "name": "string",
  "description": "string"
}'
```

<Tabs items={["200"]}>

<Tab value={"200"}>



```json title="Example Response"
{
  "id": "pfh0haxfpzowht3oi213cqos",
  "userId": "nc6bzmkmd014706rfda898to",
  "name": "string",
  "description": "string"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export interface Response {
  id?: string;
  userId?: string;
  /**
   * Name of your category
   */
  name: string;
  /**
   * Description for the category
   */
  description?: string;
}

```

</Accordion>

</Accordions>

</Tab>

</Tabs>

</APIExample>

</API>

<API>

<APIInfo method={"DELETE"} route={"/categories"}>

## Delete categories

### Request Body

Category ID to delete

<Property required={false} deprecated={false} name={"id"} type={"string"}>



<span>Example:  `"cjld2cjxh0000qzrmn831i7rn"`</span>

<span>Format:  `"cuid"`</span>

</Property>

| Status code | Description |
| ----------- | ----------- |
| `204` | No Content |
| `401` | Unauthorized |
| `403` | Forbidden |
| `429` | Too Many Requests |

</APIInfo>

<APIExample>

```bash title="curl"
curl -X DELETE "http://briefkastenhq.com/categories" \
  -d '{
  "id": "cjld2cjxh0000qzrmn831i7rn"
}'
```



</APIExample>

</API>

</Root>